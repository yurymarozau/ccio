import { Routes } from '@angular/router';
import { WalletConnectComponent } from './walletconnet/walletconnect.component';

export const routes: Routes = [
    {
        path: '',
        component: WalletConnectComponent,
    }
];
